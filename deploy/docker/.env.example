# Postgres credentials for the local dev stack (deploy/docker/docker-compose.yml).
# Copy to deploy/docker/.env to override the built-in defaults. Never commit .env.
POSTGRES_USER=tars
POSTGRES_PASSWORD=tars_dev_password_change_me
POSTGRES_DB=tars
POSTGRES_PORT=5432
